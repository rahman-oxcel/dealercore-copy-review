# The repo is public: only the ENCRYPTED page ships. Everything below holds the
# review content in the clear and must never be committed.
ben_review_sheet.xlsx
DealerCore-Reviewed-Copy.html
old_raw.json
old_templates.json
new_templates.json
join_map.json
qa_old.json
audit.json
review_new.json
data/issues.json
assets/

# Holds the Google Sheets URL for Ben's workbook, which is readable by anyone
# with the link. In a public repo that would publish the whole review in the
# clear and defeat the point of encrypting the page. Local dev aid only.
CLAUDE.md

# 63 customer-facing subject lines: deliverable copy, not configuration.
# data/overrides.json stays tracked because it holds only placeholder names.
data/subjects.json

node_modules/
package-lock.json
site/
.staticrypt.json
.claude/
